import { Roboto, Rubik } from 'next/font/google';

export const roboto = Roboto({
  weight: '400',
  subsets: ['latin'],
});

export const rubik = Rubik({
  weight: ['400', '500', '600'],
  subsets: ['latin'],
});
